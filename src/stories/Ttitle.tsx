import React from 'react';
import styled from "@emotion/styled";

const TitleStyle=styled.h2`
  display: inline-block;
  color: black;
`;

const Title = () => {
  <TitleStyle>타이틀</TitleStyle>
};

export default Title;